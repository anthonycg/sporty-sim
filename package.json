{
  "name": "sporty-sim",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Transparent football totals simulator with probabilistic player availability.",
  "scripts": {
    "start": "node server.mjs",
    "dev": "node --watch server.mjs",
    "test": "node --test",
    "smoke": "node scripts/browser-smoke.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
