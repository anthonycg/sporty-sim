{
  "name": "sporty-sim",
  "version": "0.4.0",
  "private": true,
  "type": "module",
  "description": "Opponent-adjusted EPA football profiles, prospective validation, and drive/play totals simulation.",
  "scripts": {
    "start": "node server.mjs",
    "dev": "node --watch server.mjs",
    "test": "node --test",
    "smoke": "node scripts/browser-smoke.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
